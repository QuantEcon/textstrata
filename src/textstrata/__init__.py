"""textstrata — git-derived metrics for machine-drafted, human-edited text.

Classifies every change to every document by *who* (provenance tier) and
*what* (change category), and rolls the result up into per-document and
per-corpus metrics: composition, baseline survival, derived review state,
human churn, what the machine overwrote, what editors fix.
"""
__version__ = "0.1.0.dev0"
